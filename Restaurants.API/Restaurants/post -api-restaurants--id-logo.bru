meta {
  name: post /api/restaurants/:id/logo
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/restaurants/:id/logo
  body: multipartForm
  auth: none
}

params:path {
  id: 
}

body:multipart-form {
  file: 
}
